Place `photonvision-linuxarm64.jar` in this directory, or set:

- `PHOTONVISION_JAR_PATH=/abs/path/to/photonvision-linuxarm64.jar`
- `PHOTONVISION_JAR_URL=https://.../photonvision-linuxarm64.jar`

Gaia's `program.custom` step writes/uses:

- `inputs/photonvision-linuxarm64.jar`
