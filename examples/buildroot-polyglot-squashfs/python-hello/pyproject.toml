[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "python-hello"
version = "2.0.0"
description = "Tiny Python wheel for Gaia examples"

[tool.setuptools.packages.find]
where = ["src"]
